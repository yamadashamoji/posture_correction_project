-- posture.dbの初期化スクリプト
CREATE TABLE IF NOT EXISTS posture_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT NOT NULL,
    time TEXT NOT NULL,
    posture TEXT NOT NULL,
    note TEXT
);
